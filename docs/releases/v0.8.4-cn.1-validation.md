# v0.8.4-cn.1 验证记录

源码：`e7ebbd7972a6f7fe7992ceec33f58dafae0a1b49`；上游基线：`v0.8.4`（`70f5ed0e4757c0f69891b258171da80d324f0e18`）。Bun 1.4.2，Windows x64。

## 已通过

- 本地化、语言偏好和设置搜索：31 项单元测试。
- 新引导、编辑器状态与项目环境偏好：另 51 项单元测试，共 82 项。
- `LanguageSettingsRow.browser.tsx`：6 项 Chromium 组件测试，覆盖双向切换、重建后保留、跟随系统、窗口同步，以及用户内容/密码值保护。
- `CodeEditorPane.browser.tsx`：3 项 Chromium 组件测试。
- 全仓 `bun run typecheck`；`bun run lint`（561 个警告，0 错误）。
- 本次修改的 7 个代码文件经格式化后，定向 `oxfmt --check` 通过。
- `bun run windows-runtime:check`、`bun run migrations:check`。
- `bun run build:desktop`：Web、Server、Desktop 生产构建通过。

## 全仓检查限制

运行了 `bun run test`，但不是全绿，Turbo 在失败后中止了其他包。已观察到：

- `scripts/canary.test.ts`：2 项 POSIX 路径预期与 Windows 路径不同。
- `packages/shared/src/loginShellEnvironment.test.ts`：1 项路径预期未包括 Windows 盘符。
- 桌面相关 4 个测试文件单独复查：37 项通过、6 项失败。失败位于 `browserManager.test.ts`、`browserSessionPolicy.test.ts`、`desktopUserDataProfile.test.ts` 和 `browserAutomation/browserSessionRestore.test.ts`，涉及 POSIX `file://`/路径样例和 Unix `0600` 权限位假设。
- 上述失败涉及的实现和测试文件与上游 `v0.8.4` 相同，本 fork 未修改它们。没有为获得绿灯而删除或跳过测试。
- 全仓 `bun run fmt:check` 返回 2：本机 Git 使用 CRLF，且检查范围包含旧 `.tmp-asar` 中不可解析的临时 HTML。未对上游数千个文件或本机历史工具文件做无关重写。

专项测试、类型检查和生产构建通过不代表全仓测试通过；不声明未验证的实时服务商调用、其他平台或代码签名。

## 安装包验收

- 最终 NSIS 安装包解包后的运行时依赖检查、Windows x64 隔离启动检查通过。
- 本机从 `0.8.3-cn.1` 覆盖升级成功，安装程序返回 0。Windows 文件版本为 `0.8.4.0`，安装后的 `app.asar` SHA-256 与打包载荷一致，桌面快捷方式仍指向同一安装目录。
- 已用原生窗口检查中文欢迎引导、原有对话和完整常规设置页，完成“跟随系统中文 → English → 跟随系统中文”切换。最终保持升级前的跟随系统偏好。
- 升级前已在程序退出状态备份数据与桌面配置；备份数据库中旧文本标记数量为 0。没有将备份、凭据或对话数据库放入发布附件。
- GitHub 发布的 5 个附件名称、大小、SHA-256 与本地产物逐项一致；两个更新清单内容相同，安装包 SHA-512 与清单一致。
- 发布后使用未携带 GitHub 凭据的公开下载地址重新下载完整安装包，HTTP 200，235,897,908 字节，SHA-256 与本地产物一致。
- 发布为 `Synara v0.8.4 中文版`，标签 `v0.8.4-cn.1`，不覆盖旧发布。安装包未签名，未包含自动更新源。

安装包：`Synara-0.8.4-cn.1-x64.exe`，235,897,908 字节。

```text
SHA-256: 479da913c335860de16226b27ab92aa5e17885905e8f998e53324da8f038e0a7
```
